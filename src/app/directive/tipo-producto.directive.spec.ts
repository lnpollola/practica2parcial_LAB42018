import { TipoProductoDirective } from './tipo-producto.directive';

describe('TipoProductoDirective', () => {
  it('should create an instance', () => {
    const directive = new TipoProductoDirective();
    expect(directive).toBeTruthy();
  });
});
